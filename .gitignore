# Ruflo local secrets and runtime data
.env
.env.local
.env.*.local
!.env.example
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/

# Ruflo runtime artifacts (not source)
.swarm/
.claude/
.claude-flow/
.agents/
ruvector.db
*.db
*.db-shm
*.db-wal
.mcp.json
CLAUDE.md

# Go backend (single binary, pure Go, no CGO)
*.exe
*.out
*.test
*.prof
coverage.out
coverage.html
bin/

# Frontend (SolidJS + Vite + pnpm; lockfile stays tracked)
node_modules/
dist/
.vite/
*.tsbuildinfo

# Runtime data & logs (DB_PATH=data/flowsight.db, server logs)
data/
!data/.gitkeep
*.log

# OS / editor noise
.DS_Store
Thumbs.db

# planning-with-files (local session state)
task_plan.md
findings.md
progress.md
